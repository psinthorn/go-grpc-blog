syntax = "proto3";

package proto;

message LoginRequest {
    string Login = 1;
    string Passoword =2;
}

message AuthResponse {
    string token =1;
}

service AuthService {
    rpc Login(LoginRequest) returns (AuthResponse);
}